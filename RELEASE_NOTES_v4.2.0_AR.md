# ملاحظات الإصدار 4.2.0

- إلغاء شاشة تسجيل الدخول من بداية البرنامج؛ يفتح التثبيت الجديد مباشرة.
- إضافة خيار «طلب اسم المستخدم وكلمة المرور» داخل الإعدادات، ويمكن تفعيله أو تعطيله لاحقًا.
- تعطيل قفل الخمول عندما تكون حماية تسجيل الدخول غير مفعلة، وإعادة تفعيله تلقائيًا عند تشغيل الحماية.
- الاحتفاظ بحساب إداري احتياطي `admin / admin` للتثبيت الجديد، مع إمكانية تغيير كلمة المرور وإضافة مستخدمين مستقلين.
- حماية مفتاح قاعدة البيانات بواسطة Windows DPAPI حتى عند فتح التطبيق دون تسجيل دخول.
- إضافة ترقية تلقائية للبيانات القديمة؛ إذا كانت كلمة مرور مدير قديمة مخصصة، قد يطلبها البرنامج مرة واحدة فقط لإنشاء مفتاح الجهاز، ثم يفتح مباشرة لاحقًا.
- لا يؤدي تكرار كلمة المرور الخاطئة إلى إيقاف البرنامج أو قفل الحساب.
- استبدال قاعدة التشغيل LiteDB بالكامل بقاعدة SQLite مشفرة (`patients.sqlite3`) مع WAL ومعاملات ذرية ومفاتيح خارجية وفحص سلامة.
- إضافة ترحيل مرة واحدة من قاعدة LiteDB السابقة إلى SQLite، مع إبقاء الملف القديم دون تعديل وإلغاء الاستيراد كاملًا عند أي خطأ.
- نقل المرفقات وشعار المنشأة إلى BLOB داخل SQLite وإدخالهما في اللقطة الاحتياطية المتسقة.
- إنشاء النسخ الاحتياطية بواسطة SQLite Online Backup API بدل نسخ ملف مفتوح مباشرة.
- منع حسابات الموظف والقراءة فقط من تشغيل النسخ الاحتياطي الإداري المجدول أو تعديل حالته.
- يتضمن جميع إصلاحات الأداء والتدقيق والتنظيم الواردة في الإصدار 4.1.1.
