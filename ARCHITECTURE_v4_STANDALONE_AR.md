# معمارية الإصدار 4.2 — SQLite وفتح مباشر

## القرار التنفيذي

التطبيق سطح مكتب Windows كلاسيكي غير محزوم. ينتج ملف EXE مستقلًا، إضافة إلى معالج تثبيت EXE تفاعلي عبر WiX Toolset 6. لا يستخدم MSIX أو UWP أو App Installer.

## التقنية

- واجهة Windows Forms أصلية فوق Win32، عربية ومن اليمين إلى اليسار.
- .NET 10 LTS وC# 14، ونشر `win-x64` ذاتي الاحتواء.
- Microsoft.Data.Sqlite.Core 10.0.11 كمزود ADO.NET رسمي.
- SQLitePCLRaw.bundle_e_sqlcipher 2.1.11 لتوفير محرك SQLite المشفر؛ مفتاح القاعدة عشوائي ومحمي محليًا بواسطة Windows DPAPI.
- LiteDB 5.0.21 مكوّن قراءة انتقالي فقط لاستيراد قاعدة الإصدارات السابقة مرة واحدة، وليس قاعدة تشغيل الإصدار الجديد.
- قاعدة التشغيل `patients.sqlite3` تستخدم معاملات ذرية، مفاتيح خارجية، WAL، `synchronous=FULL`، وفحص سلامة SQLite.
- الجداول تحفظ حقول الفهرسة في أعمدة واضحة البنية، وسجل الكيان كاملًا داخل حمولة JSON مشفرة بواسطة SQLCipher. المرفقات BLOB داخل القاعدة نفسها.

## الدخول والصلاحيات

عند التثبيت الجديد ينشأ حساب `admin / admin` خلفيًا، لكن التطبيق يفتح مباشرة ولا تظهر شاشة دخول ولا يعمل قفل الخمول. يستطيع المدير تفعيل «طلب تسجيل الدخول عند التشغيل» من الإعدادات. بعد التفعيل تُطبّق صلاحية الحساب المختار: مدير أو موظف أو قراءة فقط، ويعمل قفل الخمول. لا يوجد إيقاف أو قفل للحساب بسبب تكرار كلمة مرور خاطئة.

## الاستمرارية والترحيل

عند اكتشاف `patients.db` من LiteDB مع عدم وجود بيانات داخل SQLite، يقرأه التطبيق فقط ويستورد المرضى والمواعيد والمهام والمرفقات والإعدادات والإجازات والتدقيق داخل معاملة واحدة. الفشل يلغي المعاملة بالكامل ويبقي المصدر القديم دون تعديل.

النسخ الاحتياطية تستخدم SQLite Online Backup API لإنشاء لقطة متسقة، ثم SHA-256 للقاعدة وملف المصادقة. الاستعادة تفحص البصمات و`PRAGMA integrity_check` قبل الاستبدال.

## التوافق والتوزيع

الإصدار يستهدف Windows 10 وWindows 11 x64. لا يدعم Windows 7 أو 8 لأن .NET 10 لا يدعمهما. EXE غير موقّع وفق طلب المالك؛ قد يعرض SmartScreen تحذير سمعة على تنزيل جديد، لكن التطبيق لا يحتاج شهادة لكي يعمل.
