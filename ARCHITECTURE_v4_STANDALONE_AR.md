# معمارية الإصدار 4.0 المستقل

## القرار التنفيذي

الإصدار الأساسي هو تطبيق سطح مكتب Windows كلاسيكي **غير محزوم**. ينتج البناء ملفًا واحدًا باسم `Saudi-Patient-Records.exe` ولا ينتج MSIX أو UWP أو MSI ولا يستخدم Windows App Installer.

## التقنية

- واجهة Windows Forms أصلية تعمل فوق Win32 وباتجاه عربي كامل من اليمين إلى اليسار.
- أحدث إصدار LTS مستقر من `.NET 10` مع لغة C# 14.
- نشر `win-x64` ذاتي الاحتواء؛ لا يحتاج جهاز المستخدم إلى تثبيت .NET مسبقًا.
- نشر Single File مع تضمين مكتبات التشغيل الأصلية وضغطها داخل EXE واحد.
- LiteDB 5.0.21، وهو أحدث إصدار مستقر؛ لا يُستخدم الإصدار 6 التجريبي لحماية توافق قاعدة البيانات.
- حماية بيانات Windows DPAPI من الحزمة الرسمية `System.Security.Cryptography.ProtectedData 10.0.11`.
- لا يوجد قص آلي للكود `Trimming` لأن التطبيق يعتمد على ربط كائنات وLiteDB، وإيقاف القص يمنع أعطال الانعكاس وقت التشغيل.

## البيانات والتوافق

يستمر التطبيق في استخدام `%LocalAppData%\SaudiPatientRecords`، لذلك لا يضيع سجل قائم عند استبدال EXE. عمليات الحفظ تستخدم قاعدة بيانات محلية مشفرة وسجل كتابة، والنسخ الاحتياطي مستقل عن مكان ملف التشغيل.

هذا الإصدار الحديث مخصص لـ Windows 10 وWindows 11 بمعمارية x64. Windows 7 وWindows 8 غير مدعومين بواسطة .NET 10 ولا يمكن الجمع بين أحدث منصة ودعمهما في الملف نفسه.

## التوقيع

الإصدار الشخصي غير موقّع حسب الطلب ولا يحتاج شهادة كي يعمل. قد يعرض Windows SmartScreen تحذير سمعة عند تنزيل أي EXE جديد غير موقّع من الإنترنت؛ تغيير التقنية أو الامتداد لا يستطيع إلغاء سياسة Windows هذه. لا يطلب التطبيق صلاحية مسؤول، ويعمل بمستوى المستخدم العادي.

## البناء والتحقق

يشغّل `publish-standalone.ps1` اختبارات قواعد البيانات والتحقق السعودي والسعة والتعارضات، ثم ينشر ملف EXE واحدًا، يتحقق من رأس PE، ويشغّل فحصًا ذاتيًا من نفس الملف، ويصدر بصمة SHA-256 ومعلومات البناء.
