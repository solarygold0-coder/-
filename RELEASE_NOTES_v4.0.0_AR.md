# ملاحظات الإصدار 4.0.0

- تحويل التطبيق إلى Win32 كلاسيكي غير محزوم.
- إزالة MSIX وUWP وWindows App Installer ومسارات التوقيع من الإصدار الشخصي.
- ترقية التطبيق والنواة إلى .NET 10 LTS وC# 14.
- نشر ذاتي الاحتواء لمعمارية Windows x64؛ لا يحتاج المستخدم إلى تثبيت .NET.
- دمج التطبيق والنواة وLiteDB وملفات التشغيل في EXE واحد.
- تحديث Windows DPAPI إلى System.Security.Cryptography.ProtectedData 10.0.11.
- استبدال System.Web القديم بمكتبة System.Text.Json المضمنة في .NET 10 مع المحافظة على قراءة ملف الحسابات السابق.
- إضافة دعم Per-Monitor V2 DPI والمسارات الطويلة ومستوى تشغيل مستخدم عادي `asInvoker`.
- إضافة فحص ذاتي داخل EXE واختبار قاعدة البيانات والحماية عند البناء.
- الإبقاء على مسار البيانات السابق كي تعمل السجلات الموجودة دون ترحيل.

الملف المطلوب: `Saudi-Patient-Records.exe`.
