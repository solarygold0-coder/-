# ملاحظات الإصدار 5.0.0

- بنية تشغيل جديدة مستقلة كليًا عن ملفات الإصدارات السابقة.
- مجلد بيانات جديد: `%LOCALAPPDATA%\SaudiPatientRecordsV5`.
- لا نسخ تلقائيًا لـ `auth.dat` أو `database.key` أو قواعد البيانات القديمة.
- قاعدة SQLite مشفرة ومفتاح جديد خاص بالإصدار الخامس.
- فتح مباشر دون اسم مستخدم أو كلمة مرور في التثبيت الجديد.
- إزالة حساب `admin` وكلمة مروره الافتراضية بالكامل؛ يبدأ مخزن المستخدمين فارغًا.
- لا يمكن تفعيل شاشة الدخول حتى ينشئ المستخدم حساب مدير مسمى من الإعدادات.
- يبقى تفعيل تسجيل الدخول خيارًا إداريًا لاحقًا لكل المستخدمين والصلاحيات.
- اسم Mutex جديد لمنع تداخل عملية الإصدار الخامس مع العمليات القديمة.
- إزالة تسجيل التشغيل التلقائي القديم `SaudiPatientRecords` واستبداله باسم مستقل `SaudiPatientRecordsV5`.
- مسار تثبيت جديد `%LOCALAPPDATA%\Programs\Saudi Patient Records 5` مع استمرار معالج الترقية في إزالة حزمة البرنامج السابقة.
- لا تُحذف بيانات النسخ القديمة تلقائيًا حتى لا تضيع سجلات حقيقية؛ هي فقط معزولة ولا تتحكم في الإصدار الخامس.
