# معمارية الإصدار 2.0

## القرار

التطبيق المكتبي الأساسي يستخدم **WPF على .NET Framework 4.7.2** للحفاظ على تشغيل قاعدة البرنامج الحالية على Windows 7 SP1 وWindows 8.1 وWindows 10 وWindows 11. لا تُجمع WinUI 3 وWPF وAvalonia وMAUI وUno في واجهة واحدة؛ فهي بدائل لطبقة العرض.

## الطبقات

- `PatientRecordsSaudi.Core`: نماذج المجال، التحقق السعودي، قاعدة LiteDB المشفرة، الصلاحيات، التدقيق، والمرفقات والنسخ الاحتياطي. لا تعتمد على WinForms أو WPF.
- `PatientRecordsSaudi.Wpf`: واجهة WPF عربية من اليمين إلى اليسار، بنمط MVVM وخدمات نوافذ رقيقة.
- `PatientRecordsSaudi`: واجهة WinForms السابقة، أبقيناها مؤقتًا كمسار رجوع حتى اكتمال اختبار التكافؤ الوظيفي.
- `PatientRecordsSaudi.Tests`: اختبارات النواة، ومنها السعة 10,000 مراجع والتعارضات والتحقق السعودي.

## التوسع

يمكن إضافة `PatientRecordsSaudi.WinUI` مستقبلًا لويندوز 10 1809/11 ويرتبط بالنواة نفسها. يمكن كذلك إضافة واجهة Avalonia أو Uno عند وجود حاجة فعلية إلى Linux أو macOS أو WebAssembly. أما .NET MAUI فهو مناسب عند اعتماد Android/iOS، وليس مطلوبًا لإنشاء ملف EXE لويندوز فقط.

## حماية البيانات

يستمر الإصدار الجديد في استخدام نفس مجلد البيانات وصيغة قاعدة البيانات، لذلك لا يحتاج المستخدم إلى ترحيل السجلات. رقم الملف لا يُعاد استخدامه، والحذف إداري/ناعم، وتُنفذ قواعد الهوية والمواعيد والتعارضات في النواة لا في الواجهة فقط.
